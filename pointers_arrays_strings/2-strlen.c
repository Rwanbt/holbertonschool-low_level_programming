#include "main.h"

/**
 * swap_int - swaps the values of two integers
 *
 * Return: void
 */
int _strlen(char *s)
{
	int count;

	temp = 0;
	while (s[count] != '\0')
	{
		count++;
	}

	return (count);
}

